This sample intentionally includes:
- a valid CPC/image pair
- a resized image that fails the integer scale check
- a CPC referencing a missing image
- an unknown label code (ALG)
- two duplicate image filenames requiring a bulk path rule

Bulk path demo rule:
D:\Legacy\Batch-A => images/Survey-1
